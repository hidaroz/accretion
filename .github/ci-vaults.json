{
  "_comment": "Vault registry used only by CI, so the eval runs against the checked-in demo vault. The path is relative and resolves against the workspace root, which is where the workflow runs.",
  "vaults": [
    {
      "id": "demo",
      "path": "demo-vault",
      "displayName": "Demo",
      "default": true,
      "gitAutoCommit": false,
      "gitAutoPush": false
    }
  ]
}
