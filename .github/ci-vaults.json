{
  "_comment": "Vault registry used only by CI, so the eval runs against the checked-in demo vault. The path is relative to this file, so the eval finds the vault from any working directory.",
  "vaults": [
    {
      "id": "demo",
      "path": "../demo-vault",
      "displayName": "Demo",
      "default": true,
      "gitAutoCommit": false,
      "gitAutoPush": false
    }
  ]
}
